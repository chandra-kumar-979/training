// Archived: original vpc/main.tf moved to terraform/archive/vpc_main.tf
// The original resource definitions are preserved in the archive directory.
// If you need to restore the original module, copy the archived files back into this directory.

// (This file intentionally left as a placeholder to avoid accidental resource creation.)

